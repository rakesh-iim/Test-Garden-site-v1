# MrGardenr Portfolio & Resource Site

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Set up your environment variables by copying `.env.example` to `.env.local`
3. Run the app:
   `npm run dev`
